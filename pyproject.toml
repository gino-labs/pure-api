[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pure_api"
version = "0.1.0"
requires-python = ">=3.6"

[project.scripts]
fb = "everpure.flashblade.cli:main"